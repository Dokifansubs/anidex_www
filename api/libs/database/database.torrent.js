'use strict';

// Gets torrents from database.
exports.get = function(options, done) {
	done(null, {});
};

// Inserts new torrent into database.
exports.put = function(options, done) {
	done(null, {});
};

// Returns a list of infohashes this tracker tracks.
exports.whitelist = function(done) {
	done(null, {});
};